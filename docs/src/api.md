# API reference

Every exported and internal docstring, pulled directly from source — see the
[Getting started](@ref), [How it works](@ref), and [Examples](@ref) pages for narrative
walk-throughs; this page is for looking up a specific signature.

```@autodocs
Modules = [SAShE]
```
